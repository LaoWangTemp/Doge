name: build

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PUBSPEC: "pubspec.yaml"

permissions:
  contents: write

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["ci"]
    types:
      - completed
  push:
    branches:
      - "master"
    paths:
      - ".github/workflows/build.yml"

jobs:
  pre:
    runs-on: ubuntu-24.04
    outputs:
      pubspec: ${{ env.PUBSPEC }}
    steps:
      - run: echo "Bypass GitHub Action Restriction"

  ready:
    needs:
      - pre
    uses: ./.github/workflows/get-info.yml
    with:
      path: ${{ needs.pre.outputs.pubspec }}
      target: ${{ needs.pre.outputs.target }}

  build-mobile:
    runs-on: macos-latest
    needs:
      - pre
      - ready
    strategy:
      matrix:
        target: [ios, android]
        include:
          - target: ios
            build: "build ios --release --no-codesign"
          - target: android
            build: "build apk --release"

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up Flutter
        uses: subosito/flutter-action@v2.18.0
        with:
          channel: stable
          flutter-version-file: pubspec.yaml
      -
        run: flutter --version
      -
        run: flutter pub get
      -
        run: flutter test
      - 
        name: Build for ${{ matrix.target }}
        run:
          flutter ${{ matrix.build }}
        shell: bash
      - 
        if: matrix.target == 'android'
        run: |
          RELEASE_NAME="${{ needs.ready.outputs.name_lower }}"
          PREFIX="{{ needs.ready.outputs.release }}"
          EXTENSION="${PREFIX##*.}"
          BIN="$RELEASE_NAME-${{ needs.ready.outputs.version }}-${{ matrix.target }}.$EXTENSION"

          mv "{{ needs.ready.outputs.release }}" $BIN
          gh release upload ${{ needs.ready.outputs.version }} $BIN --clobber

        shell: bash
        name: Upload
      - 
        if: matrix.target == 'ios'
        run: |
          RELEASE_NAME="${{ needs.ready.outputs.name_lower }}"
          PREFIX="{{ needs.ready.outputs.release }}"
          EXTENSION="${PREFIX##*.}"
          BIN="$RELEASE_NAME-${{ needs.ready.outputs.version }}-${{ matrix.target }}"

          mv "{{ needs.ready.outputs.release }}" $BIN.$EXTENSION
          mkdir -p dist
          hdiutil create -volname "$BIN" -srcfolder "$BIN.$EXTENSION" -ov -format UDZO "$BIN.dmg"
          gh release upload ${{ needs.ready.outputs.version }} $BIN.dmg --clobber
        shell: bash
        name: Upload